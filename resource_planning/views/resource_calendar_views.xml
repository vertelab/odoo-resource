<odoo>
  <data>

    <record id="resource_calendar_form_add_points" model="ir.ui.view">
        <field name="name">resource.calendar.form.add.points</field>
        <field name="model">resource.calendar</field>
        <field name="inherit_id" ref="resource.resource_calendar_form" />
        <field name="arch" type="xml">
            <field name="hours_per_day" position="before">
                <field name="points" />
            </field>
        </field>
    </record>

  </data>
</odoo>
